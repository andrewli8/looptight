## What this changes

## Why

## Checklist

- [ ] `looptight verify` passes (the project's test command)
- [ ] `ruff check` is clean
- [ ] One focused idea, with tests
- [ ] No new runtime dependency (looptight runs on the standard library)
